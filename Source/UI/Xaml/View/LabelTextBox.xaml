<UserControl
    x:Class="StarRailDamage.Source.UI.Xaml.View.LabelTextBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:StarRailDamage.Source.UI.Xaml.View"
    xmlns:Control="clr-namespace:StarRailDamage.Source.UI.Control">
    <UserControl.Template>
        <ControlTemplate TargetType="{x:Type UserControl}">
            <Border CornerRadius="15" Background="{Binding Background,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}">
                <Grid Width="200" Height="30" Margin="5,0,5,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Foreground" Value="{Binding Foreground,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>
                            <Setter Property="FontSize" Value="{Binding FontSize,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>
                            <Setter Property="FontWeight" Value="{Binding FontWeight,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                        
                        <Style TargetType="{x:Type Control:ScopedTextBox}">
                            <Setter Property="Foreground" Value="{Binding Foreground,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>
                            <Setter Property="Background" Value="{Binding BorderBrush,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>
                            <Setter Property="FocusBrush" Value="{Binding FocusBrush,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>
                            <Setter Property="FontSize" Value="{Binding FontSize,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>
                            <Setter Property="FontWeight" Value="{Binding FontWeight,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="CornerRadius" Value="10"/>
                            <Setter Property="Padding" Value="5,0,5,0"/>
                        </Style>
                    </Grid.Resources>

                    <Image Grid.Column="0" Width="25" Height="25" Source="{Binding Icon,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1" MaxWidth="50" Text="{Binding Title,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}"/>

                    <Control:ScopedTextBox Grid.Column="3" Width="80" Text="{Binding Text,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}},Mode=TwoWay}" IsReadOnly="{Binding ReadOnly,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}" TextAlignment="Right"/>

                    <TextBlock Grid.Column="4" Width="25" Text="{Binding Unit,RelativeSource={RelativeSource AncestorType={x:Type local:LabelTextBox}}}" TextAlignment="Center"/>
                </Grid>
            </Border>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
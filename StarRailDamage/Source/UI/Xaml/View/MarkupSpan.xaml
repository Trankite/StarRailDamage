<UserControl
    x:Class="StarRailDamage.Source.UI.Xaml.View.MarkupSpan"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:StarRailDamage.Source.UI.Xaml.View"
    xmlns:suxcp="clr-namespace:StarRailDamage.Source.UI.Xaml.Control.Panel">
    <UserControl.Template>
        <ControlTemplate TargetType="{x:Type UserControl}">
            <ItemsControl ItemsSource="{Binding Items,RelativeSource={RelativeSource AncestorType={x:Type local:MarkupSpan}}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <suxcp:ScopedStackPanel Orientation="Horizontal" Spacing="{Binding Spacing,RelativeSource={RelativeSource AncestorType={x:Type local:MarkupSpan}}}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="5" Background="{Binding Background,RelativeSource={RelativeSource AncestorType={x:Type local:MarkupSpan}}}">
                            <TextBlock Margin="5,1,5,1" Text="{Binding}" Foreground="{Binding Foreground,RelativeSource={RelativeSource AncestorType={x:Type local:MarkupSpan}}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
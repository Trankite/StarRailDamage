<local:ScopedDialog
    x:Class="StarRailDamage.Source.UI.Xaml.Dialog.ConfirmDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:StarRailDamage.Source.UI.Xaml.Control.Panel"
    xmlns:suxc="clr-namespace:StarRailDamage.Source.UI.Xaml.Control"
    xmlns:suxm="clr-namespace:StarRailDamage.Source.UI.Xaml.Markup"
    Width="350" Height="200" Background="{StaticResource MainBackBrush}"
    WindowStartupLocation="CenterScreen"  ResizeMode="CanMinimize">
    <local:ScopedDialog.DataContext>
        <Binding RelativeSource="{RelativeSource Self}"/>
    </local:ScopedDialog.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Header}" FontSize="{StaticResource HighFontSize}" HorizontalAlignment="Center"/>

        <local:ScopedScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden">
            <TextBlock Text="{Binding Message}" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap"/>
        </local:ScopedScrollViewer>

        <Grid Grid.Row="2">
            <Grid.Resources>
                <Style TargetType="{x:Type suxc:ScopedButton}" BasedOn="{StaticResource {x:Type suxc:ScopedButton}}">
                    <Style.Setters>
                        <Setter Property="Width" Value="80"/>
                        <Setter Property="Height" Value="30"/>
                        <Setter Property="Background" Value="{StaticResource DropBackBrush}"/>
                    </Style.Setters>
                </Style>
            </Grid.Resources>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <suxc:ScopedButton Grid.Column="1" Click="OnAcceptClick">
                <suxc:ScopedTextBlock Text="{suxm:LocalStringMarkup Name=ViewDialogConfirmActionAccept}" TipText="{Binding Text,RelativeSource={RelativeSource Self}}" TipOnlyTrim="True" TextTrimming="CharacterEllipsis"/>
            </suxc:ScopedButton>

            <suxc:ScopedButton Grid.Column="3" Click="OnCancelClick">
                <suxc:ScopedTextBlock Text="{suxm:LocalStringMarkup Name=ViewDialogConfirmActionCancel}" TipText="{Binding Text,RelativeSource={RelativeSource Self}}" TipOnlyTrim="True" TextTrimming="CharacterEllipsis"/>
            </suxc:ScopedButton>
        </Grid>
    </Grid>
</local:ScopedDialog>
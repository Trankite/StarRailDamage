<local:ScopedWindow
    x:Class="StarRailDamage.Source.UI.Xaml.Window.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:StarRailDamage.Source.UI.Xaml.Control.Panel"
    xmlns:suxc="clr-namespace:StarRailDamage.Source.UI.Xaml.Control"
    Title="Star Rail Damage" Height="720" Width="1080"
    WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Window.DataContext>
        <Binding RelativeSource="{RelativeSource Self}"/>
    </Window.DataContext>
    
    <local:ScopedWindow.Background>
        <ImageBrush ImageSource="{StaticResource Image_Backgroup}"/>
    </local:ScopedWindow.Background>
    
    <Grid>
        <suxc:ScopedTabControl ItemsSource="{Binding TabItems}" SelectedIndex="{Binding SelectedIndex,Mode=TwoWay}" TabItemClose="TabItemClose"/>
    </Grid>
</local:ScopedWindow>